using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class imdone : MonoBehaviour
{
    public Transform emppty;
    // Start is called before the first frame update
    void Start()
    {
        emppty = transform;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
